# Spotify Song Recommendation Project Configuration

#data paths
dataset_path: "dataset/data" # test dataset
output_path: "output"

#data sampling
num_json: null  # set to an integer to limit JSON slices processed (e.g. 100); null processes all

#spark config
spark_driver_memory: "4g"
spark_shuffle_partitions: 8
spark_parquet_block_size: 33554432  # 32 MB
